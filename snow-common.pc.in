prefix=${PREFIX}
exec_prefix=${PREFIX}
includedir=${PREFIX}/include
libdir=${PREFIX}/lib

Name: libsnow-common
Description: Snow engine common library
Version: ${VERSION}
URL: https://github.com/nilium/libsnow-common
Requires.private: ${PRIVATE_PKGS}
Libs: -L${LIBDIR} -lsnow-common
Libs.private: ${PRIVATE_LIBS}
Cflags: -I${PREFIX}/include
